#include "push_swap.h"

//void	indexing(struct **head)
//{
//	t_stack *point;
//	int a = 0;
//	point = *head;
//	while(point->next)
//		point=point->next;
//	int array[(*head->name)+1];
//	while(a < ((*head->name)+1))
//	{
//		array[0] = (*head->name)
//	}
//	
//}

void	print_error(void)
{
	write(2,"Error\n",6);
	exit(1);
}
